{
  "name": "supaflare-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "uv run pywrangler deploy",
    "dev": "uv run pywrangler dev --env dev",
    "start": "uv run pywrangler dev --env dev"
  },
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}
